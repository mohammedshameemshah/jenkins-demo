pipeline {
    agent any

    stages {

        stage('Welcome') {
            steps {
                echo 'Hello from Jenkinsfile'
            }
        }

        stage('Show Files') {
            steps {
                sh 'ls -la'
            }
        }

    }
}
